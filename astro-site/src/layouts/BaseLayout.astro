---
import '../styles/global.css';
import CustomCursor from '../components/interactive/CustomCursor.astro';
import EasterEggs from '../components/interactive/EasterEggs.astro';

interface Props {
  title?: string;
  description?: string;
  ogImage?: string;
}

const {
  title = "Nakyum Song | Creative Marketer & Brand Strategist",
  description = "Portfolio of Nakyum Song - Creative Marketer and Brand Strategist building memorable brand experiences",
  ogImage = "/og-image.jpg"
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site);
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />

    <!-- Primary Meta Tags -->
    <title>{title}</title>
    <meta name="title" content={title} />
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalURL} />

    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={new URL(ogImage, Astro.site)} />

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={canonicalURL} />
    <meta property="twitter:title" content={title} />
    <meta property="twitter:description" content={description} />
    <meta property="twitter:image" content={new URL(ogImage, Astro.site)} />

    <!-- Fonts - Gruppo + Sora from Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Gruppo&family=Sora:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />

  </head>
  <body>
    <CustomCursor />
    <slot />
    <EasterEggs />

    <!-- Console Easter Egg -->
    <script>
      console.log(`
 _   _       _                        ____
| \\ | | __ _| | ___   _ _   _ _ __ / ___|  ___  _ __   __ _
|  \\| |/ _\` | |/ / | | | | | | '_ \\___ \\ / _ \\| '_ \\ / _\` |
| |\\  | (_| |   <| |_| | |_| | | | |___) | (_) | | | | (_| |
|_| \\_|\\__,_|_|\\_\\\\__, |\\__,_|_| |_|____/ \\___/|_| |_|\\__, |
                  |___/                               |___/

Hey there, fellow developer!
Like what you see? Let's connect on LinkedIn.

Built with: Astro + Tailwind + GSAP
      `);
    </script>
  </body>
</html>
