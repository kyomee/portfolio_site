{
  "scripts": {
    "build": "npm --prefix astro-site run build",
    "check": "npm --prefix astro-site run check",
    "deploy": "npm run build && ./node_modules/.bin/wrangler pages deploy astro-site/dist --project-name portfolio-site --branch main --commit-dirty=true"
  },
  "dependencies": {
    "wrangler": "^4.60.0"
  }
}
